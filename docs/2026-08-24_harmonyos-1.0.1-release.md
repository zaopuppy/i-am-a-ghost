# HarmonyOS 1.0.1 发布记录

- 构建日期：2026-08-24
- 源码提交：`8f7e2e5`（`chore: bump HarmonyOS release version`）
- Bundle：`com.zero.gamehack.iamaghost`
- 版本：`1.0.1`（`versionCode` `1000001`）
- 目标 API：HarmonyOS 6.1.1 (24)
- 兼容 API：HarmonyOS 6.1.0 (23)

## 构建

执行命令：

```text
npm run prototype:harmony:release
```

命令成功完成并生成已签名的 Release 包。Hvigor 提示当前没有启用混淆；本次发布保持项目
已有的未混淆构建策略。

## 产物

| 包 | 仓库相对路径 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| 已签名 HAP | `prototypes/harmony-gate-a/entry/build/release/outputs/default/entry-default-signed.hap` | 4,084,632 字节 | `5134364B59678BBB491EC25FED36393A09F9B4BA1C1E8268039F77EFAADA7260` |
| 已签名 APP | `prototypes/harmony-gate-a/build/outputs/release/harmony-gate-a-release-signed.app` | 2,514,667 字节 | `92624F936210F900D3F51ED55F609632A704F4DDACBF75228FF52F467916FC4C` |

构建产物继续由 Git 忽略，不提交到仓库。

## 验证

- HAP 和 APP 的 `pack.info` 均标记为 Release，并报告版本 `1.0.1` / `1000001`。
- 包内 Web bundle 包含 `e2e2bbe` 引入的 Harmony LAN 状态轮询兜底，不再包含旧的
  `JSON.parse(await host.startLan())` 直接解析路径。
- `npm run test:rules`：121 项通过，0 项失败。
- 发布命令中的 TypeScript、Vite、ArkTS 编译，HAP 签名，以及 APP 打包和签名均成功完成。

## 真机冒烟

- 设备：nova 16 Pro，HarmonyOS API 26 (Beta2)。
- AGC 发布证书签名的 release HAP 经 HDC 安装时按平台策略返回 `9568322`，即发布证书
  仅供应用市场分发，不能用于本地侧载。
- 为保留 Pura X 上的市场版本，使用相同 `1.0.1` 源码和上述 release Web 资源重新生成
  debug 签名 HAP，并侧载到 nova 16 Pro；功能代码与待上架包一致，只有签名和可调试标记不同。
- 真机点击局域网说明后，勾选状态保持，页面显示“原生局域网房间服务已就绪”，原生层成功
  监听端口并注册 mDNS；“创建房间”恢复可用。
- 点击“创建房间”后成功进入本机房间、展示一名房主并生成连接二维码，未再出现协议状态回退。
